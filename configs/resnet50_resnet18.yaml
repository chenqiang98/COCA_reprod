model:
  large_model:
    name: 'resnet50'
    pretrained: True
    lr: 0.00025
  small_model:
    name: 'resnet18'
    pretrained: True
    lr: 0.00025

dataset:
  name: 'ImageNet-C'
  path: './data/ImageNet-C'
  batch_size: 64

coca:
  # Norm layers to adapt (Tent uses BN-only). Set include_layernorm=true to adapt ViT LNs.
  include_batchnorm: true
  include_layernorm: false
  include_groupnorm: false
  include_instancenorm: false 